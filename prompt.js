//A diferenças entre window.alert(), window.confirm(), window.prompt(),

// Exercício com o window.prompt()
const num1 = prompt("Digite um primeiro número:");
const num2 = prompt("Digite um segundo número:");
const resultado = Number(num1) + Number(num2);
alert(`O resuldato foi: ${resultado}`);
